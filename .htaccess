RewriteEngine On
RewriteBase /mindarena_forum/

# Serve existing files and directories directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route everything else to the public index inside Views/public
RewriteRule ^(.*)$ Views/public/$1 [L]
